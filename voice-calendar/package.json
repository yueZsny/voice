{
  "name": "voice-calendar",
  "version": "1.0.0",
  "private": true,
  "description": "Voice Calendar MVP — 语音日历",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build:extension": "cd extension && npm install && npx vite build --config vite.config.ts"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
